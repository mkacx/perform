node_modules/
.env
dist/
*.log
/templates
azure-pipelines.yml
claude.md
instructions.md

# postman: ignore the whole folder (working files, drafts)
# Distribution: password-protected /quickstart.zip lives at project root (tracked)
/postman/